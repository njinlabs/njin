@component('layouts/main')
  @slot('title')
    njin
  @end

  @slot('main')
    <div class="min-h-screen flex items-center justify-center px-6">
      <div class="text-center max-w-lg">
        <h1 class="text-4xl font-bold tracking-tight mb-3">Welcome to njin</h1>
        <p class="text-slate-500 mb-8">
          Edit <code class="px-1.5 py-0.5 bg-slate-100 rounded text-sm">src/views/pages/index.edge</code>
          to get started, then add your first model in
          <code class="px-1.5 py-0.5 bg-slate-100 rounded text-sm">src/models/</code> and register it in
          <code class="px-1.5 py-0.5 bg-slate-100 rounded text-sm">config.ts</code>.
        </p>
        <a href="https://github.com/njinlabs/njin" class="text-violet-600 hover:text-violet-700 font-medium">
          Read the docs →
        </a>
      </div>
    </div>
  @end
@end
