# dependencies
node_modules/

# sandbox for testing the package split (bun link) — not shipped.
# All project-specific runtime state (db data, uploads, .env, build output) lives
# inside playground/ now, not at the package root, so it's covered by this alone.
playground/

# typescript
*.tsbuildinfo

# test coverage output (bun test --coverage)
coverage/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
